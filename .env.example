# Local development environment
# Copy this file to .env and update the values before running the backend.

PORT=3000
PGUSER=postgres
PGHOST=localhost
PGDATABASE=auth_dashboard
PGPASSWORD=postgres
PGPORT=5433
JWT_SECRET=glass-dashboard-secret-2024
FRONTEND_URL=http://localhost:8080

# Optional: alternative connection string for hosted PostgreSQL
# DATABASE_URL=postgres://postgres:postgres@localhost:5433/auth_dashboard
